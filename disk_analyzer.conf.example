# ==============================================================================
# disk_analyzer.sh 配置文件
# ==============================================================================
# 此配置文件用于自定义 disk_analyzer.sh 的默认行为
# 配置文件路径: 当前目录下的 disk_analyzer.conf
# 也可以通过环境变量 CONFIG_FILE 指定其他路径
# ==============================================================================

# ------------------------------------------------------------------------------
# Docker 数据目录配置
# ------------------------------------------------------------------------------
# 自定义 Docker 数据目录路径
# 如果 Docker 数据目录不在默认位置（/var/lib/docker），请在此指定
# 示例:
#   DOCKER_DATA_DIR="/data/docker"
#   DOCKER_DATA_DIR="/opt/docker/data"
# DOCKER_DATA_DIR=""

# ------------------------------------------------------------------------------
# 指定目录扫描模式配置
# ------------------------------------------------------------------------------
# 默认扫描深度（子目录层级）
# SCAN_DEPTH=3

# 默认 Top N 数量
# SCAN_TOP=20

# ------------------------------------------------------------------------------
# 全盘扫描模式配置
# ------------------------------------------------------------------------------
# 大文件扫描超时（秒）
# LARGE_FILE_SCAN_TIMEOUT=30

# 是否启用实时 I/O 负载快照（true/false）
# ENABLE_REALTIME_IO="false"

# ------------------------------------------------------------------------------
# 报告输出配置
# ------------------------------------------------------------------------------
# 报告输出路径（可选，默认为 /tmp/disk_report_$(date '+%Y%m%d_%H%M%S').md）
# REPORT_PATH="/var/log/disk_report.md"
