# 配置文件（每台服务器可能不一致，不提交到版本控制）
# 统一配置文件：所有扫描脚本共用一份（见 server-scan.conf.example）
server-scan.conf

# 旧版配置文件（统一配置前的遗留，仅作兼容回退，已不推荐使用）
disk_analyzer.conf

# 通知配置（含 webhook 与签名密钥，禁止提交）
notify.conf

# 扫描产物（报告与告警 JSON，位于 OUTPUT_DIR，不提交）
output/

# 临时文件
*.tmp
*.swp
*.swo
*~

# 操作系统文件
.DS_Store
Thumbs.db

# 报告文件
/tmp/disk_report_*.md
